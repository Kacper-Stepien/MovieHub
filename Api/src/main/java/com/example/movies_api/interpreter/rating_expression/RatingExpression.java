package com.example.movies_api.interpreter.rating_expression;

/**
 * Expression interface for rating calculations - part of Interpreter pattern
 */
public interface RatingExpression {
    double interpret();
}
